p#notice
  = notice
.row[style="width: 100%; text-align: center; padding-top: 32px;"]
  p
    = image_tag(@user.image_url, size: "48", alt: current_user.nickname, class: 'animated infinite bounce img-circle')
  h4
    | &#64;
    = @user.nickname